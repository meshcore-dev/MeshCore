// Common setUp and tearDown implementations for Unity test framework
// This file must be compiled for both Arduino and native builds

extern "C" {
  /**
   * \brief Setup function called before each test
   */
  void setUp(void) {
    // Set up before each test
  }

  /**
   * \brief Teardown function called after each test
   */
  void tearDown(void) {
    // Clean up after each test
  }
}
